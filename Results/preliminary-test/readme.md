This is the first preeliminary test done.

The purpose of this test was just to asses the behavior of the training pipeline, and make sure that all the process was running correctly without throwing any error. Thus, we defined the initial architecture of our network, a 3D convolutional network with the following layers:

